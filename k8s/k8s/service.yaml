apiVersion: v1
kind: Service
metadata:
  name: devops-service
spec:
  type: NodePort
  selector:
    app: devops-app #
  ports:
    - port: 80
      targetPort: 8080 # Puerto donde escucha aplicación Java/Spring
      nodePort: 31234